<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CapstoneDesign_GEO" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.CapstoneDesign_GEO" parent="Base.Theme.CapstoneDesign_GEO" />

    <style name="Widget.My.Spinner" parent="Widget.AppCompat.Spinner">
        <item name="android:overlapAnchor">true</item>          <!-- 드랍다운 메뉴가 스피너와 겹치는지 여부 -->
        <item name="android:spinnerMode">dropdown</item>        <!-- 드랍다운 형식과 팝업(다이얼로그)형태 선택 -->
        <item name="android:dropDownWidth">match_parent</item>  <!-- 드랍다운 메뉴 가로 길이 설정 -->
    </style>

    <style name="AppTheme.Custom" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>